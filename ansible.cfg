[defaults]
# host_key_checking = False
# deprecation_warnings = False

[inventory]
# use the implicit localhost inventory (no inline comma syntax)
enable_plugins = host_list, script, auto, yaml, ini, toml
