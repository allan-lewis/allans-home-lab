{% set buckets = s3_mirror_buckets | default([]) %}
{% if buckets is string %}
{%   set buckets = buckets.split(',') %}
{% endif %}

{% for b in buckets %}
{% set b = b | trim %}
{% if b %}
{{ b }}
{% endif %}
{% endfor %}