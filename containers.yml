---
- name: Ensure that Portainer is up and running
  hosts: docker_nodes
  become: yes
  gather_facts: false

  roles:
    - portainer 

- name: Ensure that Homer is up and running
  hosts: homer_nodes
  become: yes
  gather_facts: false

  roles:
    - homer
