---
- name: Run Docker and commmon containers 
  hosts: docker_nodes
  become: yes
  gather_facts: false

  roles:
    - docker
    - portainer
    - syncthing
    - prometheus_node_exporter
    - cadvisor
