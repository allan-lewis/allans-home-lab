
storage:
  type: sqlite
  path: /data/data.db

endpoints:
{% for target in gatus_ping_targets | default([]) %}
  - name: ping-{{ target.name }}
    group: ping
    url: "icmp://{{ target.address }}"
    conditions:
      - "[CONNECTED] == true"
{% endfor %}
