## AHL
---
movies_volume: "{{ torrent_movies}}"
shows_volume: "{{ torrent_shows }}"
music_volume: "{{ torrent_music }}"
books_volume: "{{ torrent_books }}"
torrent_downloads: "{{ transmission_downloads }}"
torrent_watch: "{{ transmission_watch }}"
jackett:
  port: 31100
  config: "{{ ahl_docker_volume_root }}/jackett"
  downloads: "{{ jackett_downloads }}"
radarr:
  port: 31300
  config: "{{ ahl_docker_volume_root }}/radarr"
readarr:
  config: "{{ ahl_docker_volume_root }}/readarr"
sonarr:
  port: 31200
  config: "{{ ahl_docker_volume_root }}/sonarr"
transmission:
  port: 31800
  config: "{{ ahl_docker_volume_root }}/transmission"
lidarr:
  config: "{{ ahl_docker_volume_root }}/lidarr"
