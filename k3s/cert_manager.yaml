## AHL
---
- name: Install cert-manager.
  hosts: k3s_cert_manager
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"

  roles:
    - role: cert_manager
