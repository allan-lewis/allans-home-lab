---
- name: Create namespace
  kubernetes.core.k8s:
    state: present
    template: namespace.yml.j2
    
- name: Deploy Cloudflare tunnels
  kubernetes.core.k8s:
    state: "{{ ahl_k3s_state }}"
    template: cloudflare.yml.j2
  with_items: "{{ k3s_cloudflare.tunnels }}"