## AHL
---
- name: Install Traefik services and ingresses.
  hosts: k3s_traefik
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"
    
  roles:
    - role: traefik_services
