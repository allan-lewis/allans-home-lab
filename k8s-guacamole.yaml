## AHL
---
- name: Install Guacamole and guacd.
  hosts: homelab_talos_guacamole_nodes
  become: no
  gather_facts: no
  environment:
    K8S_AUTH_KUBECONFIG: "{{ homelab_kubernetes_kubeconfig_path }}"

  roles:
    - role: k8s_guacamole
