## AHL
---
- name: Create a MetalLB load balancer.
  hosts: homelab_k8s_metallb_nodes
  become: no
  gather_facts: no
  environment:
    K8S_AUTH_KUBECONFIG: "{{ homelab_kubernetes_kubeconfig_path }}"

  roles:
    - role: k8s_metallb
