---
- name: Configure outbound OpenVPN service(s).
  hosts: openvpn_nodes
  become: yes
  gather_facts: yes

  roles:  
    - role: openvpn  

- name: Update networking config for node(s) using the VPN.
  hosts: netplan_nodes
  become: yes
  gather_facts: false

  roles:  
    - role: netplan 
