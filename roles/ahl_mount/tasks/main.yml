---
- import_role:
    name: ahl_mkdir
  vars:
    dir_items: "{{ mount_items }}"

- name: Mount NFS volume(s)
  ansible.posix.mount:
    path: "{{ item.path }}"
    src: "{{ item.src }}"
    fstype: nfs
    opts: "resvport,{{ item.access }}"
    state: mounted
  with_items: "{{ mount_items }}"