## AHL
---
- name: Create Plex config volume.
  ansible.builtin.file:
    path: "{{ homelab_docker_plex_config_volume }}"
    state: directory
    owner: "{{ ansible_ssh_user }}"
    group: "{{ ansible_ssh_user }}"

- name: Write the Docker Compose manifest.
  ansible.builtin.template:
    src: docker-compose.yaml.j2
    dest: "{{ homelab_docker_plex_tmp }}/docker-compose.yml"

- name: Deploy the Docker Compose stack.
  community.docker.docker_compose_v2:
    project_src: "{{ homelab_docker_plex_tmp }}"
    pull: "always"
    recreate: "always"

# - name: Install the Plex Docker container.
#   community.docker.docker_container:
#     container_name: "plex"
#     image: "lscr.io/linuxserver/plex:{{ homelab_versions.docker_plex }}"
#     restart: "unless-stopped"
#     network_mode: "host"
#     volumes: 
#       - "{{ plex_config_volume }}:/config"
#       - "{{ plex_media_library }}:/media-library"
#     environment: 
#       TZ: "{{ homelab_time_zone }}"
#       PUID: "{{ homelab_user_id }}"
#       PGID: "{{ homelab_group_id }}"
