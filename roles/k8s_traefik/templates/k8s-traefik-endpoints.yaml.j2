kind: Endpoints
apiVersion: v1
metadata:
  name: {{ item.service_name }}
  namespace: {{ homelab_k8s_namespace }}
subsets:
  - addresses:
      - ip: {{ item.service_endpoint }}
    ports:
      - port: {{ item.service_port }}
