apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: {{ homelab_talos_homepage_name }}
  namespace: default
  labels:
    app.kubernetes.io/name: {{ homelab_talos_homepage_name }}
  annotations:
    kubernetes.io/service-account.name: {{ homelab_talos_homepage_name }}
