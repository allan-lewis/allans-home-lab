## AHL
---
- name: Install Homepage.
  hosts: homelab_talos_homepage_nodes
  become: no
  gather_facts: yes
  environment:
    K8S_AUTH_KUBECONFIG: "{{ homelab_kubernetes_kubeconfig_path }}"

  roles:
    - role: talos_homepage
