## AHL
---
- name: Install Prometheus, Grafana, and Alertmanager.
  hosts: homelab_talos_monitoring_nodes
  become: no
  gather_facts: yes
  environment:
    K8S_AUTH_KUBECONFIG: "{{ homelab_kubernetes_kubeconfig_path }}"
    KUBECONFIG: "{{ homelab_kubernetes_kubeconfig_path }}"
  
  roles:
    - role: talos_monitoring
