## AHL
---
- name: Install nginx.
  hosts: homelab_talos_nginx_nodes
  become: no
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ homelab_kubernetes_kubeconfig_path }}"
    
  roles:
    - role: talos_nginx
