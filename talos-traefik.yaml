## AHL
---
- name: Install Traefik.
  hosts: homelab_talos_traefik
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ homelab_kubeconfig_path }}"
    
  roles:
    - role: talos_traefik
