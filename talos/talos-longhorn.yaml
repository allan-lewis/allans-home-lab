## AHL
---
- name: Install Longhorn.
  hosts: talos_longhorn
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"
    KUBECONFIG: "{{ kubeconfig_path }}"
  
  roles:
    - role: talos_longhorn
