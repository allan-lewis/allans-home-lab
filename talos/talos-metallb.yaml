## AHL
---
- name: Create a MetalLB load balancer.
  hosts: talos_management_nodes
  become: no
  gather_facts: true
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"

  roles:
    - talos_metallb
