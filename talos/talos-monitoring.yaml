## AHL
---
- name: Install Prometheus, Grafana, and Alertmanager.
  hosts: talos_monitoring
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"
  
  roles:
    - role: talos_monitoring
