## AHL
---
- name: Install Nextcloud.
  hosts: talos_nextcloud
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"
  
  roles:
    - role: talos_nextcloud
