## AHL
---
- name: Install Portainer.
  hosts: talos_portainer
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"

  roles:
    - role: talos_portainer
