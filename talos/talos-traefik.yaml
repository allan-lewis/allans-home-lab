## AHL
---
- name: Install Traefik.
  hosts: talos_traefik
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"
    
  roles:
    - role: talos_traefik
    - role: talos_traefik_services
