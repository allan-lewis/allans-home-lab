## AHL
---
- name: Install Vaultwarden.
  hosts: talos_vaultwarden
  become: yes
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig_path }}"

  roles:
    - role: talos_vaultwarden
