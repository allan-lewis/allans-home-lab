kind: Endpoints
apiVersion: v1
metadata:
  name: {{ item.service_name }}
  namespace: {{ homelab_kubernetes_namespace_name }}
subsets:
  - addresses:
      - ip: {{ item.service_endpoint }}
    ports:
      - port: {{ item.service_port }}
